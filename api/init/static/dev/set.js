const linkFrontend = `${window.location.protocol}//${window.location.hostname}:3200`;
const linkRabbit = `${window.location.protocol}//${window.location.hostname}:3500`;
const linkSocket = `${window.location.protocol}//${window.location.hostname}:3200/admin/socketview`;
const linkLogs = `${window.location.protocol}//${window.location.hostname}:3600`;
